[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python bot.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5

[healthcheck]
# Railway will ping this port to confirm the service is up
# Must match APP_HTTP_PORT env var (default 8080)
tcpProxyApplicationPort = 8080
